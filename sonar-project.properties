# Required metadata
sonar.projectKey=cesarBere:WEILogisticManager
sonar.projectName=Qualité logicielle personal project - A Symfony website to easily prepare a WEI
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=src

# Comma-separated paths to directories with tests (optional) (Add new dir : separate with , )
sonar.tests=src/Acme/DemoBundle/Tests

# Encoding of the source files
sonar.sourceEncoding=UTF-8

# Language
sonar.language=php
